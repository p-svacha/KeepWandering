TODOS:
==============================================

- cooked meat item
- cooked beans item
- cook action on fire
- poisoning health condition
- apply from raw meat consumption (chance)

- repair mechanic: available skill check option to spend an evening, dexterity stat. chance to increase durability of an item.

- animal attack event (wolves, reusing dog toy tag)
- attacker category human/animal

- check "todo's" in whole project (poisoning/electrocution can be added now)

VARIOUS INTERMEDIARY CHANGES/FIXES BEFORE NEXT FEATURE:
- don't show stars in item slots that are not a skillcheck
- camp items shouldn't be displayed as "item will be destroyed", need extra handling. handling should be on SLOT not on item. it should instead say something "item will be removed until morning". UI CHANGE ONLY
- fuel needs to be required for fire
- fire should be animated
- when deciding on the color of an item slot if player has a specific item, don't count items already slotted
- player still moves on map when opening map in afternoon after moving in morning
- dont explicitly mention crowbar in crate encounter option

ADD PROPER DAY 0:
- game starts at home in the morning.
- At very first morning, show house behind character
- first day encounter in the morning is the LocationEncounter Home, a unique location encounter on Vector2Int(0,0). You can go back there. In the first morning at game start, only possible option is to "start journey" and moving to another tile. Later there may be other options, but probably not. Maybe a simple temporary morale boost.

SUBSTANCE END GAME:

add "days until/since deployment" to world map
add spreading to morning report
add deployment marker to world map on day 20
add spreading markers to world map
add game over when hit

ITEM TAG ADDITION:
keep current 5 star system, works very nice
add a new 6th category, for the best of the best items in a category, represented by a purple star or something. Using an item that has a purple star in the tag associated with the skill check will GUARANTEE the best possible outcome. Should play a special animation that replaces the skill check sequence when option is selected.
Could be an interesting mechanic and generally it feels good to have guaranteed success in some cases. Could also work very well for quest items/options, like cutting the fence in the fence gate encounter.

FENCE GATE ENCOUNTER

IDEAS TO START CREATING NEW (GENERIC) LOCATION ENCOUNTERS:
- add "no encounter" that can happen in any biome with different chances. (Technically normal encounter, just with no options)
- use existing sprites that are currently ununsed (from very old version, or from old evening encounter places)
- look for an item tag that isn't used often yet, and create a location encounter that incorporates it.
- look for an item that doesn't appear often yet (in loot tables / requirements / rewards, etc, just generally in the game), and create a location encounter that incorporates it.
- look for skills that aren't used often yet in encounter options (either as relevant stats in skill checks, or as threshold requirements), and create a location encounter that incorporates it.
- look for a health condition that isn't used often yet, and create a location encounter that somehow incorporates it.
- combine any of the above points

UNLOCKABLE EVENING ENCOUNTER MECHANICS:
- instead of all options being always available, hide all at first except "rest early" and "scavenge"
- camp options should only be shown if the related camp item is in the inventory
- repair option should be unlockable somehow


actual interesting game mechanics

add at least 1 more biome
candidates: town, swamp, desert, hills/mountains

more generic encounters
go to specific biome
ROUTING
go to specific point
paths/roads (allow to move 2/3 tiles)
get a device that shows hex distance from target

GATE ITEMS by LOCATION
make it clear where to get items
new item: item pinger (shows location of item)

make encounters with more persistence across many days
countdown locations

more clearly show possible outcomes?

- move morale back to stat panel
  - remove title from stat panel, use morale as sole first line

- change title from health report panel to "Health Conditions"


- Replace destruction chance with "Uses Left" on items
- In design philosophy, try to offer 3 options per encounter step
- Replace individual item tag offsets with item tag levels. Levels have a fixed difficulty reduction across the game (lvl1 = 20, lvl2 = 40, lvl3 = 60)

- rubble encounter:
  - cutting wires should REQUIRE cutting tool
  - text should make clear that clearing rubble gives access to all items

- When fortifying, state that it reduced the danger level to X

- implement electrocution health condition
  - including heart arrythmia
  - hook it to fail of cutting fence
  - hook it to fail cutting wires in collapsed building encounter

- add one more night encounter for variety

- implement severity reduction effect of medicinal herb (ItemDefs with the "SeverityReduction" property > 0 can be "applied" to all health conditions that have natural healing. if applied, the severity is reduced by that amount instantly.

- add "exhausted" health condition: -1 to all stats, removed after sleeping.
  - can be applied for rest of current day (in afternoon encounter)
  - can also be applied for following day during evening/night encounter
  - hook to failure of keep watch in city encounter.
  - hook to failure of rubble clearning in collapsed building encounter

- somehow handle that if a lot of items fit in a slot, don't show all of them (max 5?), test with consumable custom set slot

- supply stash needs to have own loot table mixed with biome loot table
  - currently loot is too bad

- NARRATIVE: it says "you wake up afer an uneventful night"even when there was a night encounter.  

- UI: if you have an item that fits in an item slot, highlight that slot a little

- in "reveal" outcome note, when exactly 1 location encounter is revealed, show the name and coordinates in the tooltip of the note.

- improve outcome notes for health conditions
  - outcome notes for bone damage, blood loss
  - sprites for health conditions
  - outcome notes when a health condition disappears

- add a unique rumour relevant for story progression.
  - add probabilities to RumourDefs
  - fix IsRepeatable in RumourDefs
  - RumourDefs that are not repeatable can link to a specific QuestDef in the RumourDef. Availability of the rumour is then also checked against the state of that quest (e.g. only available if quest is active, or only if quest is completed, etc.)
  - The linked QuestDef is also the quest that is given to the player when they choose the rumour option (instead of generic InvestigateRumourQuest).

- show biome important stats in world map

- add some night events (things that rarely happen during the night with no player impact or visuals, communicated in morning)
  - get robbed (lose random item)
  - think of 2-3 more


- add item with "scavenging" tag: flashlight (also introduces "light source" tag)
  - currently, scavenging has an item slot for that tag, but no item with the tag exists.

- new items: empty water bottle (trash), water bottle (untreated), water purification tablets
  - water bottle have a chance to turn into empty bottle when drinking
  - empty bottles can be filled at water sources or rain -> turn into untreated water bottle

- diary: basic in-game readonly UI element that basically acts as a flavourful log of everything that happened during the game.

- more option icons (same as skill check icon):
  - UI: mark options that can only be done once per day with a clock icon
  - UI: mark options that can only be done once ever with a star icon
  - add tooltips to these icons explaining what they mean

- add items to permanently increase stats

- add "home" encounter to start position. probably just as a marker on the world map, and impassable. could later be expanded so the game actually starts in this encounter, with some basic tutorial elements and where you choose starting items.

- expand and improve name generation for cities, forests and lakes

- re-add longdog companion (simplify companion system, no health system for companions, they are simply a buff with no downside (except when they die))

- UI: health condition description should show affected stats
  - it should also be located below the health report instead of next to it when hovering

- crate encounter: when peeking, make it clear that you are looking for ADDITIONAL items to the visible one. And improve text of outcome. Also when coming back while having succesfully peeked before, write it that you remember what's inside.

- improve path history rendering (the dashed line on the world map showing your path)
  - currently the points used for the line renderer are always the center of hexes
  - that means if a tile is visited twice, the lines overlap perfectly, which looks weird
  - find a solution to make it look nice and dynamic

- cooking system:
  - raw meat can somehow be cooked into cooked meat, which gives a lot of nutrition
  - give raw meat chance to apply poisoning

- UI: make health condition text a little bigger
- UI: make tooltip text a little bit bigger

- sound (game currently has no sound at all)
  - most impactful would probably be sound effects for encounter option outcomes

- when adding multiple items to inventory at once (i.e. at start of game), ensure they do not spawn on top of each other.

- render mouth differently based on morale (smile, neutral, frown)

- implement poisoning health condition
  - design condition
  - hook up to critical failure in woods foraging
  - hook up to ItemDef for raw mean (ConsumptionPoisoningChance)

- dynamic map markers and encounter labels
  - currently each encounter has a fixed label and encounter marker, which is both shown on the world map
  - it would be cool if the label and marker reflects the state of the encounter that the player left it in. For example, if the player successfully looted a crate, the crate encounter marker could change to a "looted crate" marker, and the label could change to reflect that as well.
  - this would make moving around the map in areas the player has already been be more engaging, since the player can see the impact of their actions on the world. It also adds a lot of flavour and immersion, since the world feels more reactive and alive.

- weather system
  - could integrate nicely with skill check options. (Options could have difficulty modifiers based on weather)
  - the availability of some options in encounters could be affected by weather
  - if water is expanded on, rain could be a way to fill empty water bottles
  - weather could have additional effects on the player (e.g. rain could cause a "damp" condition that reduces morale, or heat could increase thirst faster, wind could blow away items in the night, etc.)
  - probably not 100% necessary for the core gameplay, so could be added later on as an interesting additional layer of depth

- minor UI bug: when selecting option that ends time of day, the step text disappears while the screen fades to black.

- when choosing a skill check, implement a short flashy animation showing the rolled number (on a horizontal bar, with the outcome chances (critical failure/failure/partial success/success/critical success) represented by colored segments on it).
  - This also allows for additional gameplay mechanics that come in play AFTER a skill check is rolled (including visual feedback), which is currently not possible since the outcome is determined immediately after rolling.
  - Would also make skill checks a little more exciting.
  - First example of new mechanic: Add effect for "Heart arrythmia condition": 10% chance to reduce rolled number by 10 on every skillcheck.

- Encounter options might be dirrectly connected to a SpriteRenderer of the encounter. Hovering/Clicking/dragging items onto that sprite should behave exactly as when doing these things on the option itself.
- Dragging items onto SpriteRenderers on the player. I.e. I might wanna add that dragging a bandage on a wound sprite on the player tends it (as if selecting "tend" in the context menu)
- So it should be easily doable to highlight sprite rendereds as well (potential targets) while dragging something

- red circle around quest tiles and areas on world map