- make stat highlight color based on modifier strength
- always show stat modifiers in tooltip. add base value to modifiers
- temporary health condition
- red text for exposure warning in rest and stay option
- red text when hovering option that cannot be selected with reason

- finish crate event (dynamic options on each step (GetOptions()), crowbar for smash option, new spy option to items inside)

- evening/biome encounters
- location encounter markers on world map
- exposure mechanic
- draw new background for farmland
- make poisoning a temporary condition

- move health condition description box to health description entry (beside text)
- fix health condition text color
- make health condition text a little bigger
- make infection stages to defs. use that label and description for the dynamic wound report label and description
- simplify consumption properties of items (replace with ConsumeTypeDef (like "eat", "drink") with general consumption effect. if type is empty, it is not consumable)
- sound

- biome loot tables
- biome encounters

- port missions to questdefs
- make first real way to win the game

- Encounter options might be dirrectly connected to a SpriteRenderer of the encounter. Hovering/Clicking/dragging items onto that sprite should behave exactly as when doing these things on the option itself.
- Dragging items onto SpriteRenderers on the player. I.e. I might wanna add that dragging a bandage on a wound sprite on the player tends it (as if selecting "tend" in the context menu)
- So it should be easily doable to highlight sprite rendereds as well (potential targets) while dragging something